<!DOCTYPE html>
<html>

<head>
  <meta charset="utf-8">
  <title>Shoe API</title>
</head>

<body>
  <div class="container">
    <br>
    <h1 class="text-center"> Available Shoes</h1>
    <br>
    <div class="row">
      {{#each shoeInfo}}
        <div class="col-lg-3">
          <div class="card border-warning mb-3">
            <div class="card-body">
              <h4 class="card-title text-center">{{brand}}</h4>
              <ul class="list-group list-group-flush">
                <li class="list-group-item">Cash: {{cash}}</li>
                <li class="list-group-item">Color: {{color}}</li>
                <li class="list-group-item">Size: {{size}}</li>
                <li class="list-group-item">In Stock: {{in_stock}}</li>
              </ul>
            </div>
            <form class="" action="" method="post">
              <div class="text-center">
                <input class="btn btn-success btn-block" type="submit" name="" value="Buy">
              </div>
            </form>
            <br>
          </div>
          <br>
        </div>
      {{/each}}
    </div>
  </div>
</body>

</html>
